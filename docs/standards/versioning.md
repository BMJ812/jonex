# Versioning

JØNEX uses Semantic Versioning.

Before 1.0, APIs may change between minor releases. Breaking changes to plugin
manifests, IPC, persistent storage, permissions, or security boundaries require
an architecture decision record and migration notes.
